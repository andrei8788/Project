import {IShip} from '../../../../shared/interface/app.interfaceIShip';

export const ships: IShip[] = [
  {locations: ['0', '0', '0'], hits: ['', '', '']}, // positions
  {locations: ['0', '0', '0'], hits: ['', '', '']}, // ships and coordinates
  {locations: ['0', '0', '0'], hits: ['', '', '']}  // of hits
];
